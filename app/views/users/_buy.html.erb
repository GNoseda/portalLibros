<% @buy_books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.status %></td>
    <td><%= link_to 'Loose my money!', loose_path(book), method: :post, remote: true, data: { confirm: 'Are you sure? you are burning your money!'} %></td>
  </tr>
<% end %>
