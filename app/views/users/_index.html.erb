<% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.status %></td>
    <td><%= link_to 'Unbook', status0_path(book), method: :put, remote: true %></td>
  </tr>
<% end %>
